# Assets Directory

## Images

Place your site images here, including:
- `hjemmeside-12.png` - The main header logo image

You can also set a custom logo through:
WordPress Admin > Appearance > Customize > Site Identity > Logo

The theme will automatically use the custom logo if set, otherwise it will fall back to the image in this directory.
